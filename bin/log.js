export async function log(textString) {

    //convenience method (we can change this
    // and change everywhere in the program)
    // eg. could add a file logging here
    // that creates a transcript as it logs
    // to screen also

    console.log(textString)
}
